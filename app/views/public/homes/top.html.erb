
     <div class="slider">
      <div>
        <%= image_tag('dessert-3331009_1280.jpg') %>
      </div>
      <div>
        <%= image_tag('blueberry-1082500_1280.jpg') %>
      </div>
      <div>
        <%= image_tag('breakfast-2151201_1280.jpg') %>
      </div>
      <div>
        <%= image_tag('cake-1850011_1280.jpg') %>
      </div>
    </div>

    <div class="col-12 mt-5 text-center p-4 welcome-to-nagano-cake">
      <h2 class="mt-3 mb-3">ようこそ、NAGANO CAKEへ！</h2>
      <p>
        このサイトは、ケーキ販売のECサイトになります。
        会員でない方も商品の閲覧はできますが、
        購入には会員登録が必要になります。
      </p>
    </div>

    <div class="row">
      <h3 class="col-12 mt-5 mb-5 text-center">新着情報</h3>
      <% @new_products.each do |product| %>
        <div class="col-md-6 col-sm-12 text-center">
          <%= link_to product_path(product.id) do%>
            <%= attachment_image_tag product, :image, class: "mr-3 w-100 img-responsive" %>
          <% end %>
          <strong><%= product.name %></strong></br>
          <%= "#{product.price.to_s(:delimited)}円" %>
        </div>
      <% end %>
    </div>
    <div class='text-right'>
      <%= link_to "すべての商品を見る >", products_path %>
    </div>

    <div class="row">
      <h3 class="col-12 mt-5 mb-5 text-center">人気商品</h3>
      <% @new_products.each do |product| %>
        <div class="col-md-6 col-sm-12 text-center">
          <%= link_to product_path(product.id) do%>
          <%= attachment_image_tag product, :image, class: "mr-3 w-100 img-responsive" %>
          <% end %>
          <strong><%= product.name %></strong></br>
          <%= "#{product.price.to_s(:delimited)}円" %>
        </div>
      <% end %>
    </div>
    <div class='text-right'>
      <%= link_to "すべての商品を見る >", products_path %>
    </div>




